# HASLA 모바일 리플렛 — 인수인계 가이드

**문서 대상:** 하슬라 프로젝트를 이어받아 문구·이미지·연락처·팝업 등을 수정하고 배포해야 하는 담당자
**필요 지식:** 개발자가 아니어도 됩니다. GitHub 계정만 있으면 이 문서만 보고 대부분의 수정을 처리할 수 있게 썼습니다.
**서비스 주소:** https://hasla-gangneung.vercel.app
**저장소 주소:** https://github.com/shimyawn/hasla

---

## 🚦 시작 전 신호등 표 — 이 작업을 혼자 해도 되나?

수정에 앞서 이 표에서 하려는 작업의 색을 먼저 확인하세요.

- 🟢 **혼자 무리 없음** — 이 문서만 보면 됩니다.
- 🟡 **도전 가능하지만 실수 나기 쉬움** — 커밋 전에 한 번 더 확인하고, 문법 실수 시 [10번 Revert](#10-사고-쳤을-때-되돌리기)를 미리 숙지하세요.
- 🔴 **개발자 필요** — 무리하지 말고 개발자에게 넘기세요. 잘못 손대면 사이트가 뜨지 않을 수 있습니다.

| 신호 | 작업 | 참고 절 |
| :-: | --- | :-: |
| 🟢 | 상영시간 배열 수정 (`schedule.ts`) | [4-6](#4-6-상영시간표) |
| 🟢 | 스플래시 / 탭 / 지도 단일 문구 (`ui.ts`) | [4-1](#4-1-스플래시-첫-화면-문구)~[4-3](#4-3-지도-페이지-문구) |
| 🟢 | 전화번호 · 주소 (`contact.ts`) | [4-8](#4-8-연락처-주소-라벨) |
| 🟢 | 리뷰폼 · 인스타 · 네이버 플레이스 URL (`links.ts`) | [7](#7-외부-링크--전화-인스타-네이버플레이스-리뷰폼) |
| 🟢 | 이미지 교체 (동일 파일명·확장자로) | [6-1](#6-1-존별-대표-이미지)~[6-3](#6-3-지도-위-존-아이콘), [6-6](#6-6-티저-영상) |
| 🟡 | 존별 콘텐츠 스토리 · 설명 · 연출방향 (`zones.ts`) | [4-4](#4-4-존zone별-콘텐츠-스토리-설명-연출-방향-매체) |
| 🟡 | 소개 페이지 본문 문단 (`about.ts`) | [4-5](#4-5-소개about-페이지-문구) |
| 🟡 | INFO 시트 · 스플래시 하단 배너 문구 | [5-1](#5-1-info-시트-운영-안내-팝업), [5-2](#5-2-스플래시-하단-상태-배너) |
| 🟡 | 지도 핀 좌표 미세 조정 (`zones.json`의 `mapPin`) | [6-4](#6-4-지도-위-존-아이콘-위치-조정) |
| 🟡 | 관람객 참여 페이지 문구 | [4-7](#4-7-관람객-참여-리뷰-페이지) |
| 🟡 | 영어(EN) 번역 함께 반영 | [8](#8-영어en-번역-유지) |
| 🔴 | 지도 배경 이미지의 **비율(가로:세로)** 이 달라지는 교체 | [6-2](#6-2-지도-배경-이미지), [9](#9-지도--지도-리디자인의-경우) |
| 🔴 | 파비콘 · iOS 홈 아이콘 · OG 공유 이미지 (코드로 그림) | [6-5](#6-5-로고--파비콘--og-이미지) |
| 🔴 | 스플래시 로고 3장 세트 교체 (color·black·full) | [6-5](#6-5-로고--파비콘--og-이미지) |
| 🔴 | 이미지 확장자 변경 (`.png` ↔ `.jpg`) | [6](#6-이미지-교체) |
| 🔴 | 가오픈 안내(PreOpenBanner) **카드 자체를 제거** | [5-3](#5-3-가오픈-안내-카드-preopenbanner) |
| 🔴 | 존 개수 자체가 달라지는 지도 리디자인 | [9](#9-지도--지도-리디자인의-경우) |
| 🔴 | 새 페이지 추가 · SEO 메타(`layout.tsx`) 수정 | (개발자에게 전달) |
| 🔴 | 폰트 · 색상 팔레트 · 애니메이션 톤 변경 | (개발자에게 전달) |
| 🟢 | 담당자 교체(GitHub Collaborator · Vercel Member 초대) | [13-1](#13-1-시나리오-a-담당자만-교체--기존-리포기존-배포-유지) |
| 🟡 | Vercel 환경변수(전화·인스타 URL·GA ID 등) 등록·수정 | [13-5](#13-5-환경변수-목록) |
| 🟡 | 로컬 개발 환경 셋업(npm install · npm run dev) | [13-3](#13-3-시나리오-c-로컬-개발-환경-셋업-선택) |
| 🔴 | 새 계정으로 처음부터 배포 · 서비스 이관 | [13-2](#13-2-시나리오-b-새-계정으로-처음부터-배포), [13-6](#13-6-서비스-이관-시-체크리스트) |

---

## 0. 먼저 이 3가지만 이해하고 시작하세요

1. **원본 코드는 GitHub에 있습니다.** GitHub의 `main` 브랜치에 있는 파일이 곧 실서비스입니다.
2. **GitHub `main` 브랜치가 바뀌면 Vercel이 자동으로 배포합니다.** (약 1~2분)
   → 즉, "파일 수정 → 커밋(Commit)" 이 배포의 전부입니다.
3. **가장 무서운 것은 "잘못 저장했는데 이미 배포됨" 인데, 다행히 GitHub에는 되돌리기(Revert) 기능이 있습니다.** 이 문서 [10번](#10-사고-쳤을-때-되돌리기)에서 자세히 다룹니다.

---

## 1. 저장소 & 로컬 파일 구조 요약

이 폴더(로컬):
```
C:\Users\yawns\OneDrive\2025\강릉\모바일 리플렛\hasla-gangneung
```

이 폴더 안의 파일들이 GitHub 저장소(`shimyawn/hasla`)와 같습니다.

파일 수정 방법은 두 가지입니다.
- **A. 로컬에서 수정 후 커밋 (권장하지 않음, 개발 지식 필요)**
- **B. GitHub 웹에서 바로 수정 후 커밋 (권장, 이 문서의 기본 경로)** ✅

---

## 2. GitHub 웹에서 파일 수정하는 기본 흐름

이 흐름을 한 번만 손에 익혀두면 이후 모든 수정이 동일합니다.

1. https://github.com/shimyawn/hasla 접속 → 로그인
2. 상단 파일 트리에서 수정하려는 파일까지 클릭해 들어가기
   예) `src` → `i18n` → `ui.ts`
3. 파일 오른쪽 위 **연필 아이콘(Edit this file)** 클릭
4. 파일 내용을 브라우저에서 직접 수정
5. 화면 오른쪽 위 초록색 **Commit changes...** 버튼 클릭
6. 팝업이 뜨면:
   - **Commit message**: 어떤 걸 바꿨는지 한 줄로 요약 (예: `상영시간 20:00 → 20:15 수정`)
   - **아래는 그대로 두고** `Commit directly to the main branch` 선택
   - 초록색 **Commit changes** 클릭
7. Vercel이 1~2분 안에 자동 배포합니다.
8. https://hasla-gangneung.vercel.app 에서 새로고침(Ctrl+F5) → 확인

**중요:** 여러 파일을 동시에 수정할 때는, 파일 하나 수정하고 커밋 → 다음 파일 수정하고 커밋 이렇게 나눠서 진행하세요. 무엇을 언제 바꿨는지가 명확해집니다.

---

## 3. 수정 시나리오 인덱스

| 바꾸고 싶은 것 | 참고 절 |
| --- | --- |
| 스플래시(첫 화면) 문구·슬로건 | [4-1](#4-1-스플래시-첫-화면-문구) |
| 상·하단 탭 이름 | [4-2](#4-2-상단하단-탭-메뉴-이름) |
| 지도 페이지 안내 문구 | [4-3](#4-3-지도-페이지-문구) |
| 각 존(ZONE)의 제목·스토리·설명·연출 방향·매체 리스트 | [4-4](#4-4-존zone별-콘텐츠-스토리-설명-연출-방향-매체) |
| 소개(About) 페이지 문구 | [4-5](#4-5-소개about-페이지-문구) |
| 상영시간표 (19:30 / 20:00 …) | [4-6](#4-6-상영시간표) |
| 관람객 참여(리뷰) 페이지 문구 | [4-7](#4-7-관람객-참여-리뷰-페이지) |
| 연락처·주소·SNS 라벨 | [4-8](#4-8-연락처-주소-라벨) |
| INFO 시트(운영 안내 팝업) | [5-1](#5-1-info-시트-운영-안내-팝업) |
| 스플래시 하단 상태 배너 | [5-2](#5-2-스플래시-하단-상태-배너) |
| 가오픈 안내 카드(PreOpenBanner) | [5-3](#5-3-가오픈-안내-카드-preopenbanner) |
| 존 대표 이미지 (8개) | [6-1](#6-1-존별-대표-이미지) |
| 지도 배경 이미지 | [6-2](#6-2-지도-배경-이미지) |
| 지도 위 존 아이콘 (8개) | [6-3](#6-3-지도-위-존-아이콘) |
| 지도 위 존 아이콘 위치(핀 좌표) | [6-4](#6-4-지도-위-존-아이콘-위치-조정) |
| 로고 / 파비콘 / OG 이미지 | [6-5](#6-5-로고--파비콘--og-이미지) |
| 티저 영상 | [6-6](#6-6-티저-영상) |
| 전화번호 · 인스타그램 · 네이버 플레이스 · 리뷰 폼 URL | [7](#7-외부-링크--전화-인스타-네이버플레이스-리뷰폼) |
| 영어 번역 유지 | [8](#8-영어en-번역-유지) |
| 지도 자체를 새로 디자인 (구조 변경) | [9](#9-지도--지도-리디자인의-경우) |
| 되돌리기 / 사고 대응 | [10](#10-사고-쳤을-때-되돌리기) |

---

## 4. 텍스트 수정

**핵심 규칙:** 관람객에게 보이는 대부분의 문구는 `src/i18n/` 폴더에 모아뒀습니다. HTML 화면을 직접 뒤질 필요가 없습니다.

- `src/i18n/ui.ts` : 버튼·라벨·탭 이름 등 UI 문구 (가장 자주 수정)
- `src/i18n/zones.ts` : 각 존(ZONE)의 스토리·설명·연출 방향 등 콘텐츠 문구
- `src/i18n/about.ts` : 소개(About) 페이지 본문
- `src/data/schedule.ts` : 상영시간

각 파일은 **한국어(`ko`) 블록**과 **영어(`en`) 블록**이 상하로 붙어 있습니다. **한국어만 수정하면 영어는 그대로**이므로, 관람객이 EN을 눌렀을 때 어색해지지 않도록 [8번](#8-영어en-번역-유지) 참고 후 함께 수정하세요.

### 4-1. 스플래시 (첫 화면) 문구

**파일:** `src/i18n/ui.ts`

찾을 키:
- `splashLabel` — 화면 상단 라벨 (예: `강릉 이머시브 아트 쇼`)
- `splashSloganLine1` / `splashSloganLine2` — 슬로건 2줄
- `splashHint` — 화면 하단 힌트 (예: `TAP TO AWAKEN`)
- `splashCta` — 진입 버튼 문구 (예: `숲으로 들어가기`)
- `splashOpenDate` — 오픈 날짜 문구 (예: `2026년 5월 2일 가오픈`)
- `copyrightSuffix` — 하단 저작권 문구

**주의:** 슬로건 2줄은 화면에서 각각 한 줄씩 표시되므로, 너무 긴 문장은 자동 줄바꿈이 되어 어색해집니다. **한 줄 30자 이하** 권장.

### 4-2. 상단/하단 탭 (메뉴) 이름

**파일:** `src/i18n/ui.ts`

찾을 키:
- `tabHome` — 메인
- `tabAbout` — HASLA 소개
- `tabShow` — 상영시간표
- `tabReviews` — 관람객 참여

**주의:** 탭 이름이 너무 길면 모바일 하단 4개 탭에서 잘려 보일 수 있습니다. **6자 이하** 권장.

### 4-3. 지도 페이지 문구

**파일:** `src/i18n/ui.ts`

- `mapPageTitle` — 지도 페이지 제목
- `mapCaption` — 지도 상단 안내 (예: "빛나는 영역을 탭하면...")
- `mapPCEmptyEyebrow` / `mapPCEmptyTitle` / `mapPCEmptyHint` — **PC에서만** 지도 오른쪽 빈 패널에 표시되는 안내 문구

### 4-4. 존(ZONE)별 콘텐츠 (스토리·설명·연출 방향·매체)

**파일:** `src/i18n/zones.ts` (프론트에서 실제로 표시되는 문구)
**보조 파일:** `src/data/zones.json` (SEO·구조 데이터용 — 웬만하면 함께 맞춰 수정)

`zones.ts` 안에는 zone1 ~ zone8 각각에 대해 아래 필드가 있습니다.

| 필드 | 어디에 표시됨 | 주의사항 |
| --- | --- | --- |
| `title` | 존 상세 상단 제목 | 지도 카드에도 사용됨 |
| `subtitle` | 제목 아래 부제 | 지도 카드에도 사용됨 |
| `tagline` | 제목·부제 아래 한 줄 설명 | 짧게 유지 |
| `story` | 스토리 섹션 | 감성적 톤 |
| `description` | 본문 설명 | 가장 긴 문단, `\n\n` 로 문단 나눔 |
| `direction[]` | "연출 방향" 리스트 | 배열, 항목 하나당 한 줄 |
| `media[]` | "연출 매체" 리스트 | 배열, 조명·구조물·센서 등 |

**중요 — 배열(`direction`, `media`) 수정 시:**
```ts
direction: [
  '첫 번째 항목',
  '두 번째 항목',   // ← 마지막 항목 뒤 쉼표 있어도 상관없음
],
```
따옴표(`'` 또는 `"`) 짝이 안 맞으면 사이트가 배포 오류로 이전 버전이 남아있게 됩니다. **따옴표 짝**과 **쉼표**만 조심하면 됩니다.

**콘텐츠 자체를 갈아엎을 만큼 대규모 수정이 필요할 때:**
`zones.json`(SEO용)과 `zones.ts`(화면용)에 같은 정보가 중복되어 있으므로 **둘 다** 맞춰야 검색엔진과 화면이 동기화됩니다. 시간이 부족하면 우선 `zones.ts`만 수정해도 화면에는 반영됩니다.

### 4-5. 소개(About) 페이지 문구

**파일:** `src/i18n/about.ts`

이 파일은 `ABOUT_COPY.ko` / `ABOUT_COPY.en` 로 나뉘어 있고, 그 안에 섹션별 문단이 배열로 들어 있습니다. 필드 이름이 그대로 화면 순서를 나타내므로 위에서부터 순서대로 대응됩니다.

**부제/티저 라벨 등 About 페이지 상단 부분**은 `ui.ts` 쪽의 `aboutTeaserLabel`, `aboutTeaserHeading`, `aboutTeaserNote` 에 있습니다.

### 4-6. 상영시간표

**파일:** `src/data/schedule.ts`

```ts
export const showTimes = ['19:30', '20:00', '20:30', '21:00'] as const
```

이 배열 한 곳만 수정하면 아래 두 곳이 동시에 반영됩니다.
- `/show` 페이지 (상영시간표)
- `/zone/zone6` (인피니티 포레스트) 상세 페이지

**주의:** 시간 문자열은 반드시 따옴표로 감싸고, 시간 사이에 쉼표를 넣어야 합니다. 형식은 `'HH:MM'` (24시간제).

**함께 손대야 할 곳** — 시간표가 바뀌면 아래 문구도 같이 수정하는 것이 좋습니다:
- `src/i18n/ui.ts` → `infoSessionsNote` 안에 "19:30부터 21:30까지, 30분 간격 4회" 문장이 들어있음
- `src/i18n/ui.ts` → `infoPerDayLabel` (예: `1일 4타임`)
- `src/i18n/ui.ts` → `splashStatusBanner` 상태 배너 문구
- `en` 블록의 같은 키도 함께

### 4-7. 관람객 참여 (리뷰) 페이지

**파일:** `src/i18n/ui.ts`

- `reviewsIntro` — 페이지 상단 안내 문구
- `reviewsCta` — 리뷰 작성 버튼 문구

**리뷰 폼 URL 자체를 바꾸려면** → [7번](#7-외부-링크--전화-인스타-네이버플레이스-리뷰폼) 참고.

### 4-8. 연락처·주소

연락처는 두 종류로 나뉩니다:

**(A) 라벨 (문구) — `src/i18n/ui.ts`**
- `contactSectionLabel` — "문의" 섹션 라벨
- `contactName` — 상호명 (예: `하슬라강릉이머시브아트쇼`)
- `contactPhoneLabel` — "대표 전화" 라벨
- `contactAddressLabel` — "주소" 라벨
- `contactPlaceLabel`, `contactPlaceCta` — 네이버 플레이스 관련 라벨
- `contactInstagramLabel`, `contactInstagramHandle` — 인스타 라벨과 표시 계정명

**(B) 실제 값 (전화번호·주소 본문) — `src/data/contact.ts`** ✅ 비개발자 셀프 수정 가능

```ts
export const CONTACT = {
  phone: '0507-1322-4508',
  address: '강원 강릉시 초당동 474-4',
} as const
```

따옴표 안 값만 바꾸면 됩니다.
- `phone` : 화면에 표시되는 번호 + 탭했을 때 걸리는 번호 둘 다 여기서 관리
- `address` : ContactBlock에 표시되는 주소 한 줄

**함께 알아둘 점:**
- **인스타 계정** 표시 텍스트(`@hasla_5moons`)는 `ui.ts`의 `contactInstagramHandle` 에 있습니다 (한/영 각각).
- **인스타 URL**과 **네이버 플레이스 URL**은 `src/data/links.ts` 에 있습니다 ([7](#7-외부-링크--전화-인스타-네이버플레이스-리뷰폼) 참고).
- **검색엔진용 구조화 주소**(JSON-LD)는 `src/app/layout.tsx` 안에 별도로 있습니다. 실주소가 완전히 이전하는 경우 이곳도 함께 손봐야 하는데, 이건 개발자 몫으로 남기는 걸 권장합니다.

---

## 5. 팝업 / 공지사항 수정

### 5-1. INFO 시트 (운영 안내 팝업)

지도 화면 상단 오른쪽 **INFO 버튼**을 누르면 아래에서 올라오는 시트입니다.

**파일:** `src/i18n/ui.ts`

핵심 키(모두 `ko`/`en` 양쪽에 존재):
- `infoButtonLabel` — INFO 버튼 라벨
- `infoSheetTitle` — 시트 제목 (예: `운영 안내`)
- `infoPreOpenLabel` — 첫 배지 라벨 (예: `공지`)
- `infoPreOpenLineShort` — 짧은 안내 문구 (예: `지금은 가오픈 기간입니다`)
- `infoNoticeParagraphs` — **문단 배열**. 3개 문단을 각각 문자열로 넣습니다.
  ```ts
  infoNoticeParagraphs: [
    '현재 HASLA는 가오픈 기간입니다.',
    '운영 시간은 계절과 일몰 시각에 따라\n탄력적으로 변경될 수 있습니다.',
    '방문객 여러분의 너른 양해 부탁드립니다.',
  ],
  ```
  문장 안에서 줄바꿈이 필요하면 `\n` 을 넣으세요.
- `infoSessionsHeading` — "인피니티 포레스트 조명쇼 타임테이블" 헤딩
- `infoSessionsNote` — 시간표 아래 부연 문구
- `infoGrandOpenLabel`, `infoGrandOpenWhen` — "정식 운영" 라벨과 안내
- `infoCloseLabel` — 닫기 버튼

### 5-2. 스플래시 하단 상태 배너

스플래시(첫 화면) 아래쪽에 흐르는 얇은 한 줄 배너.

**파일:** `src/i18n/ui.ts`
- `splashStatusBanner` — 예: `지금은 가오픈 기간입니다 · 1일 4타임 송출 · 운영 시간은 계절·일몰에 따라 변동`

### 5-3. 가오픈 안내 카드 (PreOpenBanner)

/map, /about, /show, /feedback, /zone 상단에 흰색 카드로 뜨는 안내입니다.

**표시할 문구:** `src/i18n/ui.ts` → `infoPreOpenLabel`, `infoPreOpenLineShort` 사용

**카드 자체를 완전히 숨기고 싶다면** (정식 오픈 이후 등):
`src/components/PreOpenBanner.tsx` 파일을 열어 개발자에게 문의하세요. 각 페이지 상단에서 `<PreOpenBanner />` 라인을 지우면 사라집니다 — 하지만 파일 안 여러 곳에 있으니 여러 파일을 손봐야 합니다.

**쉬운 방법:** 문구를 "정식 개장 · 매일 19:30 첫 상영" 같은 정식 오픈 안내로 바꿔 그대로 두는 것.

---

## 6. 이미지 교체

> **디자이너에게 새 파일을 요청할 때는 [`docs/DESIGN-SPEC.md`](docs/DESIGN-SPEC.md) 를 함께 전달하세요.** 각 이미지의 목표 비율·크기·형식·명명 규칙이 파일별로 정리되어 있어 그대로 교체 가능한 파일을 받을 수 있습니다.

**모든 이미지는 `public/` 폴더 안에 있습니다.**

교체 방법 (GitHub 웹):
1. 저장소에서 `public/images/파일명.png` 경로로 들어감
2. 오른쪽 위 파일명 옆 점 3개 메뉴(⋯) → **Delete file** → 커밋
3. `public/images/` 폴더 화면으로 돌아옴 → **Add file → Upload files**
4. **동일한 파일명**으로 새 이미지 업로드 → **Commit**

**중요:**
- **파일명을 반드시 그대로 유지**하세요. 대소문자·확장자(.png / .jpg)까지 정확히 일치해야 합니다. 확장자가 바뀌어야 한다면 [코드 참조도 함께 수정](#65-확장자가-바뀌는-경우-예-png--jpg)해야 합니다.
- 저작권을 준수하고, 이미지 원본을 미리 백업해두세요.

### 6-1. 존별 대표 이미지

| 존 | 파일 경로 |
| --- | --- |
| Zone 1 하슬라 포털 | `public/images/zone1_main.jpg` |
| Zone 2 그루터기의 숨결 | `public/images/zone2_main.png` |
| Zone 3 거울 연못 | `public/images/zone3_main.png` |
| Zone 4 다섯 개의 달 | `public/images/zone4_main.png` |
| Zone 5 달의 초상 | `public/images/zone5_main.png` |
| Zone 6 인피니티 포레스트 | `public/images/zone6_main.png` |
| Zone 7 달의 잔상 | `public/images/zone7_main.jpg` |
| Zone 8 빛의 파동 | `public/images/zone8_main.png` |

**권장 규격:** 가로 1600px 이상, 세로는 자유(대개 3:2 또는 4:3). 용량 2MB 이하 추천.

### 6-2. 지도 배경 이미지

- 파일: `public/images/map.jpg`
- 지도 위 존 아이콘 위치(`mapPin`)는 **이 이미지 크기 기준의 백분율(%)** 로 저장되어 있으므로, **지도 이미지의 비율(가로:세로)** 이 바뀌면 아이콘들이 어긋납니다. 원본과 같은 비율의 이미지로 교체하는 것이 가장 안전합니다.
- 비율이 바뀌었다면 → [6-4](#6-4-지도-위-존-아이콘-위치-조정)에 따라 8개 좌표를 다시 잡아야 합니다.

### 6-3. 지도 위 존 아이콘

- 위치: `public/icons/zone1.png` ~ `zone8.png`
- **투명 배경 PNG** 로 넣어야 지도와 자연스럽게 어울립니다.
- 원본 아이콘 대비 너무 커지거나 작아지면 지도 위 시각적 균형이 깨집니다. 원본 크기 유지 권장.

### 6-4. 지도 위 존 아이콘 위치 조정

**파일:** `src/data/zones.json`

각 존 안에 `mapPin` 이라는 객체가 있습니다:
```json
"mapPin": {
  "cx": "10.5%",
  "cy": "88%",
  "w": "13.3%",
  "h": "7.9%"
}
```

- `cx`, `cy` : 아이콘의 **중심 좌표** (지도 왼쪽 위 기준 백분율)
- `w`, `h` : 아이콘의 **가로/세로 크기** (지도 크기 대비 백분율)

**수정 팁:**
- 값을 조금씩 바꿔가며 저장(커밋) → 실서비스에서 확인하는 방식으로 진행하세요.
- 개발환경(로컬)에서 실시간으로 조정하려면 개발자 도움이 필요합니다.

### 6-5. 로고 / 파비콘 / OG 이미지

| 항목 | 파일 |
| --- | --- |
| 스플래시 컬러 로고 | `public/images/logo_color.png` |
| 스플래시 실루엣 로고 | `public/images/logo_black.png` |
| 상단바 로고 (헤더) | `public/images/logo_full.png` |
| 브라우저 탭 파비콘 | `src/app/icon.tsx` (코드로 그림) — 개발자 문의 |
| iOS 홈 화면 아이콘 | `src/app/apple-icon.tsx` (코드로 그림) — 개발자 문의 |
| 카톡·페이스북 공유 OG 이미지 | `src/app/opengraph-image.tsx` (코드로 그림) — 개발자 문의 |

**스플래시 로고 3가지(color/black/full)의 관계는 문라이즈 애니메이션과 얽혀 있으므로**, 새 로고로 교체하는 경우 3장 모두 같은 컨셉으로 준비해서 함께 갈아끼우세요. 잘못 넣으면 애니메이션이 어색해집니다.

**확장자가 바뀌는 경우 (예: png → jpg)**
파일명 참조가 코드 안에 흩어져 있어 개발자 도움을 받는 것이 안전합니다.

### 6-6. 티저 영상

- 파일: `public/videos/teaser.mp4`
- About 페이지의 티저 섹션에서 재생됩니다.
- **권장 규격:** 세로 영상(9:16 또는 4:5). 30~60초. 20MB 이하.
- **음성이 있는 영상**은 iOS에서 자동 재생되지 않고 관람객이 재생 버튼을 눌러야 합니다.

---

## 7. 외부 링크 (전화·인스타·네이버플레이스·리뷰폼)

**파일:** `src/data/links.ts`

```ts
export const LINKS = {
  naverPlace: '...',
  instagram: '...',
  reviewForm: '...',
} as const
```

- `naverPlace` : 지도 위 "네이버 플레이스" 링크
- `instagram` : 인스타그램 링크
- `reviewForm` : 관람객 참여 페이지의 "리뷰 작성" 버튼이 열어주는 폼(구글 폼)

**따옴표 안 URL만 바꾸면 됩니다.** 문법이 어렵진 않지만, 따옴표를 지우지 않도록 주의.

**전화번호는 여기가 아닙니다.** → [4-8 하단 안내](#4-8-연락처-주소-라벨) 참고.

---

## 8. 영어(EN) 번역 유지

`ui.ts`, `zones.ts`, `about.ts` 는 **한국어(`ko`)와 영어(`en`) 블록이 상하 쌍으로** 되어 있습니다.

관람객이 우측 상단의 **LANGUAGE** 토글을 눌러 EN을 선택하면 en 블록의 값이 그대로 표시됩니다.

**따라서 한국어를 수정하면 반드시 영어 쪽도 함께 수정해야** 관람객이 EN을 눌렀을 때 옛 내용이 남아있지 않습니다.

**팁:**
- 파일 안에서 `ko:` 블록을 먼저 수정한 후, `en:` 블록에서 같은 이름의 키(같은 `identifier`)를 찾아 함께 고치세요.
- 급하게 반영해야 하는데 번역이 준비 안 됐다면, 우선 한국어만 고쳐 배포한 뒤 영어 번역이 오는 대로 추가 커밋으로 반영해도 됩니다.

---

## 9. 지도 — 지도 리디자인의 경우

> 디자이너에게 지도·아이콘 리디자인을 요청할 때는 [`docs/DESIGN-SPEC.md`](docs/DESIGN-SPEC.md) 의 § 1, § 2 를 함께 넘겨주세요. 지도 비율(1600:1748)과 8개 존 아이콘의 목표 비율표가 정리되어 있어 코드 수정 없이 파일만 갈아끼울 수 있습니다.

지도 이미지 자체를 완전히 새로 디자인하는 경우:

**시나리오 A. 지도 그림만 바뀌고 8개 존 위치는 그대로**
→ [6-2](#6-2-지도-배경-이미지)만 교체하면 끝. **단, 새 지도의 세로 비율이 원본과 다르면 아이콘들이 어긋나므로 [6-4](#6-4-지도-위-존-아이콘-위치-조정) 좌표 조정 필요.**

**시나리오 B. 지도 위 존의 배치·개수 자체가 달라짐**
→ 개발자 도움 필요. 아래 3곳을 손봐야 합니다.
1. `public/images/map.jpg` — 새 지도 이미지
2. `public/icons/zone1.png ~ zone8.png` — 필요 시 새 아이콘
3. `src/data/zones.json` — 각 존의 `mapPin` 좌표 재조정 및 (존 개수가 달라지면) 존 배열 자체를 조정

**시나리오 C. 지도가 완전히 다른 인터랙션 (예: 스크롤형, 3D 등)**
→ 개발자와 전체 재설계 필요. 이 문서 범위를 벗어납니다.

---

## 10. 사고 쳤을 때 되돌리기

**증상 1: 배포 후 사이트가 멀쩡히 뜨는데, 내가 바꾼 문구가 이상해 보임**
→ 그냥 다시 그 파일로 가서 수정하고 다시 커밋하세요. 정상 배포 흐름 안입니다.

**증상 2: 배포 후 사이트에 접속하면 에러 화면이 뜬다 / 이전 버전만 계속 남아있다**
→ 방금 커밋에서 문법 실수(따옴표 짝 안 맞음, 쉼표 빠짐 등)를 냈을 가능성이 큽니다.
Vercel이 새 배포에 실패하면 **이전 성공 버전이 그대로 노출**되므로, 관람객 눈에는 큰 문제 없이 이전 버전이 보입니다. 여유 있게 아래 절차로 원상복구하면 됩니다.

**되돌리는 방법 (GitHub 웹):**
1. https://github.com/shimyawn/hasla/commits/main 에서 최근 커밋 이력 확인
2. 방금 잘못 만든 커밋을 클릭
3. 오른쪽 위 점 3개(⋯) → **Revert**
4. 자동으로 뜨는 화면에서 **Commit changes** → 이전 상태로 되돌아감
5. Vercel이 다시 자동 배포하여 이전 정상 상태로 복원됩니다.

**증상 3: 실수로 파일을 통째로 지웠거나 큰 변경을 되돌리고 싶음**
→ 위와 동일한 **Revert** 절차로 대부분 해결됩니다. Git은 모든 이력을 남기므로 "완전히 사라지는" 경우는 거의 없습니다. 확신이 없다면 무리하지 말고 개발자에게 문의하세요.

---

## 11. 절대 건드리지 말 것 (모른다면)

이 파일들은 앱의 뼈대라서 잘못 건드리면 사이트가 뜨지 않습니다. 개발자 없이 열지 마세요.

- `package.json`, `package-lock.json` — 라이브러리 목록
- `next.config.ts`, `tsconfig.json`, `postcss.config.mjs` — 빌드 설정
- `src/app/layout.tsx` — 루트 레이아웃 (SEO 메타 포함)
- `src/app/SplashClient.tsx` — 스플래시 애니메이션 로직
- `src/app/globals.css` — 전역 CSS
- `src/components/` 안 대부분의 `.tsx` — UI 컴포넌트 코드
- `src/fonts/` — 폰트 파일 (재직접 서명 필요)
- `scripts/repair-loca.py` — 폰트 복구 스크립트

**단, 이 문서의 4~7번에서 명시적으로 언급한 파일**은 안전하게 수정 가능합니다.

---

## 12. 자주 묻는 질문

**Q. 커밋을 했는데 사이트에 반영이 안 됩니다.**
- 1~2분 기다린 뒤 브라우저를 강력 새로고침(Ctrl+F5) 하세요.
- 그래도 안 되면 Vercel 대시보드(https://vercel.com/) 에 접속해 최근 Deployment 상태 확인. **Build Failed** 라면 방금 커밋에 문법 오류가 있는 것 → [10번](#10-사고-쳤을-때-되돌리기)의 Revert.

**Q. 한 파일 안에서 여러 곳을 동시에 바꿔도 되나요?**
- 됩니다. 다만 실수 확률이 늘어나므로, 각 변경 단위(예: "스플래시 문구 수정", "상영시간 수정")마다 커밋을 나눠주면 나중에 되돌리기 편합니다.

**Q. 미리보기(스테이징)를 볼 수 있나요?**
- GitHub에서 **Pull Request** 를 만들면 Vercel이 자동으로 임시 URL(Preview) 을 발급해줍니다. 개발자에게 부탁하거나, GitHub 기본 사용법을 익힌 뒤 시도하세요.
- 간단한 문구 수정은 대부분 곧바로 main 에 커밋해도 되지만, **큰 폭의 콘텐츠 개편**은 반드시 Preview에서 먼저 확인하세요.

**Q. 검색엔진에 반영은 언제 되나요?**
- 콘텐츠 변경 후 네이버·구글에서 재수집(크롤링)까지는 짧으면 하루, 길면 1~2주 걸립니다. 서치어드바이저(네이버) / 서치콘솔(구글) 에서 "URL 검사 → 색인 요청" 을 걸면 앞당길 수 있습니다.

**Q. 로컬(내 PC)에서 미리보기를 열고 싶어요.**
- `npm install` → `npm run dev` → http://localhost:3000 접속 (Node.js 20+ 필요). 개발 지식이 필요하므로 개발자 문의를 권합니다.

**Q. Vercel · GitHub 로그인 정보는 어디에?**
- 저장소 소유자(shimyawn) 계정 관리자에게 문의. 새 담당자가 배포까지 관리하려면 **GitHub 협업자(Collaborator)** 초대와 **Vercel 팀 초대**가 필요합니다.

**Q. 사이트에 관해 더 깊이 이해하고 싶어요.**
- 프로젝트 개발자를 위해 남긴 `PROJECT-CONTEXT.md` 를 참고. 왜 이렇게 짜여있는지, 무엇을 시도하고 되돌렸는지 등이 정리돼 있습니다.

---

## 13. GitHub · Vercel 연결과 새 배포 셋업

담당자가 바뀌었을 때 GitHub/Vercel 를 어떻게 이어받거나, 상황에 따라 새 계정으로 다시 세팅하는지 정리했습니다. **세 가지 시나리오** 중 본인 상황에 맞는 것만 따라가면 됩니다.

- [시나리오 A. 담당자만 교체 — 기존 리포·기존 배포 유지 (권장)](#13-1-시나리오-a-담당자만-교체-기존-리포기존-배포-유지)
- [시나리오 B. 새 계정으로 처음부터 배포 (예: 조직 변경)](#13-2-시나리오-b-새-계정으로-처음부터-배포)
- [시나리오 C. 로컬 개발 환경 셋업 (선택)](#13-3-시나리오-c-로컬-개발-환경-셋업-선택)

### 13-1. 시나리오 A. 담당자만 교체 — 기존 리포·기존 배포 유지

**가장 안전한 경로입니다.** URL(https://hasla-gangneung.vercel.app), SEO 이력, 검색엔진 색인이 모두 그대로 유지됩니다.

**전 담당자가 해야 할 일:**

1. **GitHub Collaborator 초대**
   - https://github.com/shimyawn/hasla 접속 → **Settings** → **Collaborators** → **Add people**
   - 새 담당자의 GitHub 계정명 또는 이메일 입력 → 초대 발송
   - 권한은 기본 **Write** 로 충분 (이 문서의 모든 작업 가능)

2. **Vercel Team 초대**
   - https://vercel.com/ 로그인 → 프로젝트 이름 옆 조직명 클릭 → **Settings** → **Members** → **Invite**
   - 새 담당자 이메일 입력 → 초대 발송
   - 권한 **Member** 로 충분 (배포 로그 조회, 환경변수 관리 가능)

3. **인수인계용 정보 전달**
   - GitHub 저장소 URL
   - Vercel 프로젝트 URL (대시보드)
   - 실서비스 URL (https://hasla-gangneung.vercel.app)
   - 이 `HANDOFF.md`, `PROJECT-CONTEXT.md` 위치 안내
   - **[13-5 환경변수 목록](#13-5-환경변수-목록)** 을 함께 확인하여 새 담당자에게도 값 공유

**새 담당자가 해야 할 일:**

1. **초대 수락** — GitHub·Vercel에서 각각 이메일의 초대 링크 클릭
2. **접근 테스트** — GitHub에서 아무 `.md` 파일(예: `README.md`)을 열어 **연필 아이콘**이 보이는지 확인
3. **첫 커밋 테스트** — HANDOFF.md 하단 어딘가에 공백 하나 추가 → 커밋 → **Vercel 대시보드에서 새 Deployment가 자동으로 시작되는지** 확인. 실서비스에 영향 없는 안전한 테스트입니다.
4. 확인 후 그 공백 커밋은 다시 되돌리거나 그대로 두어도 무방.

---

### 13-2. 시나리오 B. 새 계정으로 처음부터 배포

이 경우 URL이 새로 발급되고, **SEO 이력·검색엔진 색인·Vercel Analytics 지표가 모두 초기화**됩니다. 실서비스 URL을 바꾸지 않아도 되는 상황이라면 [시나리오 A](#13-1-시나리오-a-담당자만-교체-기존-리포기존-배포-유지)를 강력 권장합니다.

**Step 1. GitHub 새 저장소 만들기**

1. **기존 리포지토리를 새 계정으로 옮기는 두 방법:**
   - **(권장) Transfer** — 원 소유자(shimyawn) 계정에서 저장소 **Settings → Transfer ownership** → 새 계정으로 이관. 커밋 이력·Star·이슈가 모두 유지되며 URL만 바뀝니다.
   - **(대안) Fork/Clone** — 새 계정에서 Fork하거나, 로컬로 clone 후 새 저장소에 push. 이력을 새로 시작하는 방식.

**Step 2. Vercel 프로젝트 생성**

1. https://vercel.com/ 에서 새 계정으로 로그인 (또는 팀 생성)
2. **Add New → Project** 클릭
3. GitHub 저장소 목록에서 방금 준비한 저장소 선택 → **Import**
4. 프로젝트 설정 화면에서:
   - **Framework Preset**: `Next.js` (자동 감지됨)
   - **Root Directory**: `./` 또는 자동
   - **Build Command / Output Directory**: 기본값 그대로
5. **Environment Variables** 영역에서 아래 [13-5 환경변수 목록](#13-5-환경변수-목록) 참고해 필요한 값 입력
6. **Deploy** 클릭 → 첫 배포 완료까지 2~5분

**Step 3. 실서비스 URL(SITE_URL) 변경**

새 Vercel 배포의 URL이 예전과 다르다면(예: `hasla-gangneung-v2.vercel.app`) 아래 4곳의 하드코딩된 URL을 함께 수정해야 검색엔진·SNS 공유가 정상 동작합니다.

| 파일 | 수정 위치 |
| --- | --- |
| `src/app/layout.tsx` | 상단 `const SITE_URL = 'https://hasla-gangneung.vercel.app'` |
| `src/app/sitemap.ts` | 상단 `const SITE_URL = ...` |
| `src/app/robots.ts` | (있다면) sitemap URL 참조 |
| `PROJECT-CONTEXT.md` | Production URL 문구 |

**Step 4. 커스텀 도메인 연결 (선택)**

Vercel 대시보드 → 프로젝트 → **Settings → Domains → Add**에서 원하는 도메인 등록. DNS 관리 페이지에서 안내된 A 레코드 또는 CNAME 설정 필요.

**Step 5. 검색엔진 재등록**

기존 검색엔진 색인은 새 URL로 자동 이관되지 않습니다. 다음 절차 필요:

- **Google Search Console** (https://search.google.com/search-console)
  - 새 URL로 속성 추가 → HTML 파일 인증(파일 다운로드)
  - 다운받은 인증 HTML을 `public/` 폴더에 업로드 → 커밋 → 배포
  - 기존 `public/googleb9db032ad24ffb6a.html` 은 삭제 가능
  - 인증 완료 후 **Sitemaps** 메뉴에서 `sitemap.xml` 제출
- **네이버 서치어드바이저** (https://searchadvisor.naver.com)
  - 마찬가지로 새 URL 등록 → HTML 파일 인증
  - 기존 `public/naverd72d7ba9cebe08b306b7134a1b8c7825.html` 삭제 가능

---

### 13-3. 시나리오 C. 로컬 개발 환경 셋업 (선택)

문구/이미지만 수정한다면 [2번](#2-github-웹에서-파일-수정하는-기본-흐름)의 GitHub 웹 편집만으로 충분합니다. 로컬 환경은 지도 핀 좌표 세부 조정처럼 **실시간 미리보기가 절실할 때**만 준비하세요.

**준비물:**
- **Node.js 20 이상** (https://nodejs.org/ — LTS 버전 권장)
- **Git** (https://git-scm.com/)
- **에디터** — VS Code (https://code.visualstudio.com/) 추천

**셋업 절차:**

```bash
# 1. 저장소 클론 (한 번만)
git clone https://github.com/shimyawn/hasla.git
cd hasla

# 2. 의존성 설치 (한 번만, 3~5분 소요)
npm install

# 3. 개발 서버 실행
npm run dev
```

- http://localhost:3000 접속하여 확인
- 코드를 저장하면 브라우저가 자동 새로고침
- 종료: 터미널에서 `Ctrl+C`

**주의:**
- 로컬에서 수정한 내용을 실서비스에 반영하려면 `git commit` + `git push` 필요
- `git push` 하면 곧바로 Vercel이 자동 배포하므로, [Pull Request 로 Preview 확인 후 병합](#12-자주-묻는-질문) 흐름이 안전

---

### 13-4. Vercel 대시보드 사용법 (최소한만)

배포 상태 확인이 필요할 때 3가지만 알면 됩니다.

1. **Deployments 탭** — 최근 배포 이력. 초록 체크 = 성공, 빨간 × = 실패
2. **실패한 배포 클릭 → Build Logs** — 에러 원인이 콘솔 로그로 출력됨. 대개 오탈자·따옴표 짝 문제
3. **Settings → Environment Variables** — 아래 [13-5](#13-5-환경변수-목록) 항목을 관리하는 곳

**배포 실패 대응:**
- 대시보드에서 실패 커밋 확인 → GitHub에서 그 커밋을 [Revert](#10-사고-쳤을-때-되돌리기) → 이전 성공 배포가 자동으로 다시 활성화됨.
- 관람객에게는 계속 이전 성공 버전이 노출되므로 여유롭게 대응 가능.

---

### 13-5. 환경변수 목록

Vercel **Settings → Environment Variables** 에서 등록하는 값들입니다. 코드 안에 이미 기본값이 하드코딩되어 있어서 **필수는 아니고, 코드 수정 없이 값만 바꾸고 싶을 때** 사용합니다.

| 환경변수 | 역할 | 없을 때 기본값 |
| --- | --- | --- |
| `NEXT_PUBLIC_CONTACT_PHONE` | 대표 전화번호 | `0507-1322-4508` |
| `NEXT_PUBLIC_CONTACT_ADDRESS` | 주소 표시 문구 | `강원 강릉시 초당동 474-4` |
| `NEXT_PUBLIC_NAVER_PLACE_URL` | 네이버 플레이스 링크 | `https://naver.me/xy7JAsef` |
| `NEXT_PUBLIC_INSTAGRAM_URL` | 인스타그램 링크 | `https://www.instagram.com/hasla_5moons/` |
| `NEXT_PUBLIC_REVIEW_URL` | 관람객 리뷰 폼 링크 | (구글 폼 URL) |
| `NEXT_PUBLIC_GA_ID` | Google Analytics 4 ID (`G-XXXXXXXX`) | 미설정 시 GA4 미로드 |

**등록 방법 (Vercel):**
1. 대시보드 → 프로젝트 → **Settings** → **Environment Variables**
2. 이름 (예: `NEXT_PUBLIC_CONTACT_PHONE`) + 값 입력
3. 적용 환경 체크: **Production** / **Preview** / **Development** 모두 선택 권장
4. **Save** → 다음 배포부터 반영. 즉시 반영하려면 **Deployments** 에서 최근 배포 옆 **⋯ → Redeploy**

**주의:**
- **`NEXT_PUBLIC_` 접두사**가 있어야 브라우저에서 읽힙니다. 서버 전용 값은 접두사 없이(더 안전) 사용하지만, 현재 프로젝트는 관람객 페이지에 그대로 노출되는 값뿐이라 전부 `NEXT_PUBLIC_` 로 통일했습니다.
- **API 키, 비밀번호 등 민감정보는 이 프로젝트에 없습니다.** 나중에 추가된다면 절대 `NEXT_PUBLIC_` 붙이지 말고, Vercel의 Production 환경에만 등록하세요.

---

### 13-6. 서비스 이관 시 체크리스트

시나리오 B(새 계정으로 배포)를 진행할 때 놓치기 쉬운 항목:

- [ ] Vercel 프로젝트 배포 완료 및 새 URL 확인
- [ ] `SITE_URL` 상수 3곳(layout.tsx, sitemap.ts, robots.ts) 수정
- [ ] 환경변수 6개 모두 Vercel에 등록 (필요한 것만이라도)
- [ ] Google Search Console 재등록 + sitemap 제출
- [ ] Naver 서치어드바이저 재등록 + sitemap 제출
- [ ] (있다면) 커스텀 도메인 DNS 연결
- [ ] SNS 공유 미리보기 테스트 (https://www.opengraph.xyz/ 에 새 URL 붙여넣어 이미지 렌더링 확인)
- [ ] 리치 결과 테스트 (https://search.google.com/test/rich-results — Event 구조가 파싱되는지)
- [ ] 인쇄물 · QR 코드 · SNS 프로필의 URL 링크 교체 요청 (관리 주체에게 별도 안내)

---

## 14. 손대기 전 체크리스트

수정을 시작하기 전에 아래 3가지를 지키면 사고 확률을 크게 줄입니다.

- [ ] 원본(수정 전) 파일 내용을 캡처하거나, 커밋 이력이 있으니 [Revert 가능](#10-사고-쳤을-때-되돌리기)함을 인지
- [ ] 한국어 수정 시 → 같은 파일 `en:` 블록도 함께 반영
- [ ] 상영시간표 수정 시 → INFO 시트(`infoSessionsNote`, `infoPerDayLabel`) 문구도 함께 확인
- [ ] 이미지 교체 시 → 파일명·확장자를 원본과 동일하게 유지
- [ ] 지도 이미지 교체 시 → 원본과 같은 가로:세로 비율 유지 (아니면 8개 좌표 재조정)
- [ ] 커밋 메시지는 "무엇을 왜 바꿨는지" 한 줄로 명확히
- [ ] 커밋 후 1~2분 뒤 https://hasla-gangneung.vercel.app 에서 실제 반영 확인

---

**작성일:** 2026-07-01
**대상 브랜치:** `main` (자동 배포)
**참고 문서:** `README.md`, `PROJECT-CONTEXT.md`, `AGENTS.md`
